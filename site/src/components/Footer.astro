<footer class="site-footer">
  <div class="footer-grid">

    <!-- Left: A project by Rhodes College -->
    <div class="footer-col footer-col--left">
      <span class="footer-label">A project by</span>
      <a href="https://www.rhodes.edu" target="_blank" rel="noopener noreferrer">
        <img src="/assets/RhodesLogo.png" alt="Rhodes College" class="footer-logo footer-logo--rhodes" />
      </a>
    </div>

    <!-- Center: Contact -->
    <div class="footer-col footer-col--center">
      <span class="footer-label">Contact Us</span>
      <a href="mailto:perovsat@gmail.com" class="footer-contact-link">perovsat@gmail.com</a>
      <span class="footer-contact-detail">Rhodes Tower 401</span>
    </div>

    <!-- Right: In collaboration with NRL -->
    <div class="footer-col footer-col--right">
      <a
        href="https://github.com/PEROVSAT"
        target="_blank"
        rel="noopener noreferrer"
        class="footer-github-link"
      >
        <img src="/assets/GitHubWhiteIcon.webp" alt="" class="footer-github-icon" />
        PEROVSAT GitHub
      </a>
    </div>

  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--rhodes-border);
    background: var(--rhodes-surface);
    color: var(--rhodes-text-muted);
    font-size: 0.875rem;
    padding: 2rem 1.5rem 1.25rem;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    align-items: center;
    gap: 1.5rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .footer-col {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    text-align: center;
  }

  .footer-col--left  { align-items: center; }
  .footer-col--right { align-items: center; }

  .footer-label {
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--rhodes-text-muted);
  }

  /* Rhodes logo — white-on-black, so give it a dark pill background */
  .footer-logo--rhodes {
    height: 90px;
    width: auto;
    background: #111;
    border-radius: 6px;
    padding: 8px 14px;
    object-fit: contain;
  }

  .footer-logo--nrl {
    height: 90px;
    width: auto;
    object-fit: contain;
  }

  .footer-contact-link {
    color: var(--rhodes-accent, #b00);
    text-decoration: none;
    font-weight: 600;
  }

  .footer-contact-link:hover {
    text-decoration: underline;
  }

  .footer-contact-detail {
    font-size: 0.82rem;
    color: var(--rhodes-text-muted);
  }

  .footer-join {
    font-style: italic;
    max-width: 18rem;
  }

  .footer-github-link {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    margin-top: 0.25rem;
    color: var(--rhodes-text-muted);
    text-decoration: none;
    font-weight: 600;
  }

  .footer-github-link:hover {
    color: var(--rhodes-accent, #b00);
    text-decoration: underline;
  }

  .footer-github-icon {
    width: 1.25rem;
    height: 1.25rem;
    object-fit: contain;
  }

  /* Responsive: stack vertically on small screens */
  @media (max-width: 640px) {
    .footer-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }
</style>
