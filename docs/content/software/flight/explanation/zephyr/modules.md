# Modules

!!! warning "Under Construction"
    This article is not written yet.

Topics to cover when this page is written:

- What a Zephyr **module** is (out-of-tree code integrated into the build)
- How modules appear in the **West manifest** and workspace layout
- PEROVSAT custom modules (drivers, libraries) vs upstream Zephyr modules
- `module.yml` and how Zephyr discovers and builds modules
- When to add a module vs keeping code in the application repository
