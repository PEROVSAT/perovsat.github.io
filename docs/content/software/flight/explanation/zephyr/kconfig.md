# Kconfig

!!! warning "Under Construction"
    This article is not written yet.

Topics to cover when this page is written:

- What **Kconfig** is and how it differs from DeviceTree (compile-time vs hardware description)
- **`prj.conf`** — where application and project options live
- Enabling kernel subsystems, drivers, and stack sizes
- How Kconfig choices interact with DeviceTree (`CONFIG_*` vs hardware nodes)
- PEROVSAT conventions for naming and organizing options
- Link to official Zephyr Kconfig reference for syntax and search (`menuconfig`)
