# Global flags

!!! warning "Under Construction"
    This article is not written yet.

Topics to cover when this page is written:

- What **global flags** are and why we use them alongside message queues
- **`DEPLOY_COMPLETE`** — semantics, who sets it, who consumes it
- Other mission-critical flags and their lifetimes
- Thread-safety and ordering concerns on an RTOS
- When to use a flag vs IPC for cross-thread state
