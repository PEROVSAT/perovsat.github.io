# Operational status

!!! warning "Under Construction"
    This article is not written yet.

Topics to cover when this page is written:

- **`OP_STATUS`** states: SAFE, LOW, NOMINAL, HIGH — plain-language meaning of each
- What triggers transitions between states (deploy, faults, ground command, EPS constraints)
- What each state **restricts or permits** (power draw, payload ops, comms cadence)
- How threads read and react to operational status
- Relationship to safe-mode booting and FDIR (with links to reliability docs)
